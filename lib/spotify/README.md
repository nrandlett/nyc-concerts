# lib/spotify

Helpers for building Spotify embed URLs so users can preview an artist's music
directly on a concert listing. This is embed-only (no auth / Web API needed for
the basic player), so it stays lightweight.

_Empty for now — embed helpers come in a later session._
